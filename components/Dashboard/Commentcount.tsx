import { AllComments } from "@/app/[project]/[commentId]/(actions)/fetchcomment";
import { Fetchsingleproject } from "@/app/Dashboard/(actions)/fetchsingleproject";

export default async function CommentCount({ id }: { id: string }) {
  return <div>{message} comment</div>;
}
